"""The algorithms, one package each.

An algorithm is a choice at each of the seven pipeline stages, plus the
loop that runs them and — where the standard accountants do not cover it
— its own accounting. Nothing is re-exported here; import from the
algorithm's own modules.
"""

__all__: list[str] = []
